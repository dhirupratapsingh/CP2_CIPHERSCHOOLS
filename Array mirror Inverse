#include<bits/stdc++.h>
using namespace std;
bool abc(int a[],int n)
{
	for(int i=0;i<n;i++)
	{
		if(a[i]>n-1)
		{
			return false;
		}
		if(a[a[i]]!=i)
		{
			return false;
		}
	}
}
int main()
{
	int n;
	cin>>n;
	for(int i=0;i<n;i++)
	{
		cin>>a[i];
	}
	bool ans=true;
	ans=abc(a,n);
}
